name: Build ISOs

on:
  #schedule:
  #  - cron: '05 10 */14 * 2'
  workflow_dispatch:
    inputs:
      platform:
        required: true
        type: choice
        options:
          - amd64
          - arm64
  pull_request:
    branches:
      - main

env:
  IMAGE_NAME: ${{ github.event.repository.name }}
  IMAGE_REGISTRY: "ghcr.io/${{ github.repository_owner }}"
  DEFAULT_TAG: "latest"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref || github.run_id }}
  cancel-in-progress: true

jobs:
  build:
    name: Build ${{ matrix.variant }} ISO
    runs-on: ${{ inputs.platform == 'amd64' && 'ubuntu-24.04' || 'ubuntu-24.04-arm' }}

    strategy:
      fail-fast: false
      matrix:
        variant: [standard, nvidia]

    permissions:
      contents: write
      packages: write
      id-token: write

    steps:
      - name: Prepare environment
        run: |
          echo "IMAGE_REGISTRY=${IMAGE_REGISTRY,,}" >> ${GITHUB_ENV}
          echo "IMAGE_NAME=${IMAGE_NAME,,}" >> ${GITHUB_ENV}
          echo "USER_UID=$(id -u)" >> ${GITHUB_ENV}
          echo "USER_GID=$(id -g)" >> ${GITHUB_ENV}

      - name: Install dependencies (ARM only)
        if: inputs.platform == 'arm64'
        run: |
          sudo apt update -y
          sudo apt install -y podman

      - name: Maximize build space
        if: inputs.platform != 'arm64'
        uses: ublue-os/remove-unwanted-software@cc0becac701cf642c8f0a6613bbdaf5dc36b259e
        with:
          remove-codeql: true

      - name: Checkout
        uses: actions/checkout@v4

      # 🔥 Build ISO using titanoboa
      - name: Build ISO
        id: build
        uses: RakuOS/titanoboa@main
        with:
          image-ref: ghcr.io/rakuos/${{ env.IMAGE_NAME }}${{ matrix.variant == 'nvidia' && '-nvidia' || '' }}:latest
          hook-post-rootfs: ${{ github.workspace }}/config/configure-iso.sh
          hook-pre-initramfs: ${{ github.workspace }}/config/pre_initramfs.sh
          compression: squashfs
          builder-distro: fedora

      # 🔥 Rename ISO with UTC date
      - name: Rename ISO
        if: github.event_name != 'pull_request'
        shell: bash
        run: |
          BUILD_DATE=$(date -u +%Y.%m.%d)
          if [[ "${{ matrix.variant }}" == "nvidia" ]]; then SUFFIX="-nvidia"; else SUFFIX=""; fi
          mkdir -p output
          NEW_NAME="rakuos-kde${SUFFIX}-${BUILD_DATE}-${{ inputs.platform }}.iso"
          mv ${{ steps.build.outputs.iso-dest }} output/${NEW_NAME}
          cp output/${NEW_NAME} output/rakuos-kde${SUFFIX}-latest-${{ inputs.platform }}.iso
          echo "NEW_ISO_NAME=${NEW_NAME}" >> $GITHUB_ENV
          echo "BUILD_DATE=${BUILD_DATE}" >> $GITHUB_ENV

      # 🔥 Generate SHA256 checksums
      - name: Generate SHA256 checksum
        if: github.event_name != 'pull_request'
        shell: bash
        run: |
          if [[ "${{ matrix.variant }}" == "nvidia" ]]; then SUFFIX="-nvidia"; else SUFFIX=""; fi
          cd output
          sha256sum "$NEW_ISO_NAME" > "$NEW_ISO_NAME.sha256"
          sha256sum "rakuos-kde${SUFFIX}-latest-${{ inputs.platform }}.iso" > "rakuos-kde${SUFFIX}-latest-${{ inputs.platform }}.iso.sha256"

      # 📦 Upload artifacts to GitHub
      - name: Upload disk images and checksum to Job Artifacts
        if: github.event_name != 'pull_request'
        uses: actions/upload-artifact@v4
        with:
          name: rakuos-kde-${{ matrix.variant }}-${{ inputs.platform }}
          path: |
            output/${{ env.NEW_ISO_NAME }}
            output/${{ env.NEW_ISO_NAME }}.sha256
          if-no-files-found: error
          retention-days: 7
          compression-level: 0
          overwrite: true

      # 🌐 Connect to ZeroTier
      - name: Join ZeroTier Network
        if: github.event_name != 'pull_request'
        uses: zerotier/github-action@v1
        with:
          network_id: ${{ secrets.ZEROTIER_NETWORK_ID }}
          auth_token: ${{ secrets.ZEROTIER_CENTRAL_TOKEN }}

      # ⏳ Wait for ZeroTier interface
      - name: Wait for ZeroTier IP
        if: github.event_name != 'pull_request'
        run: |
          sleep 10
          ip addr show
          ping -c 3 ${{ secrets.SFTP_HOST }}

      # 🗑️ Remove previous ISOs from server
      - name: Remove previous ISOs from server
        if: github.event_name != 'pull_request'
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SFTP_HOST }}
          username: ${{ secrets.SFTP_USERNAME }}
          password: ${{ secrets.SFTP_PASSWORD }}
          script: |
            SUFFIX=""
            [[ "${{ matrix.variant }}" == "nvidia" ]] && SUFFIX="-nvidia"
            cd "${{ secrets.SFTP_TARGET }}"
            rm -f rakuos-kde${SUFFIX}-[0-9]*-${{ inputs.platform }}.iso
            rm -f rakuos-kde${SUFFIX}-[0-9]*-${{ inputs.platform }}.iso.sha256
            rm -f rakuos-kde${SUFFIX}-latest-${{ inputs.platform }}.iso
            rm -f rakuos-kde${SUFFIX}-latest-${{ inputs.platform }}.iso.sha256

      # 🚀 Upload to SFTP
      - name: SFTP upload via ZeroTier
        if: github.event_name != 'pull_request'
        uses: Dylan700/sftp-upload-action@latest
        with:
          server: ${{ secrets.SFTP_HOST }}
          username: ${{ secrets.SFTP_USERNAME }}
          password: ${{ secrets.SFTP_PASSWORD }}
          port: 22
          uploads: |
            output/ => ${{ secrets.SFTP_TARGET }}/

      # 📤 Upload ISOs to SourceForge
      - name: Upload to SourceForge
        if: github.event_name != 'pull_request'
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SFTP_HOST }}
          username: ${{ secrets.SFTP_USERNAME }}
          password: ${{ secrets.SFTP_PASSWORD }}
          script: |
            if [[ "${{ matrix.variant }}" == "nvidia" ]]; then
              sf-upload.sh --nvidia kde ${{ env.BUILD_DATE }}
            else
              sf-upload.sh kde ${{ env.BUILD_DATE }}
            fi
